package cs3500.animator.controller;

/**
 * An animation controller responds to user inputs and controls the timing of an animation.
 * Once created, the controller is what starts the application.
 */
public interface AnimationController {

  /**
   * Starts the animation program based on view and model given.
   */
  void start();

}
